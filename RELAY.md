# RELAY — 인계 보드

> ## ✉️ 다음 세션에게 (먼저 읽어라)
> 이번 세션(2026-05-16) 코드는 거의 다 작성·푸시됐다. **너의 1번 임무는 새 기능이 아니라 검증이다.**
> 1. **EAS 빌드 + Vercel 배포 후 시각확인이 핵심** — 코드는 typecheck/9번까지만(L2~3). 진짜 됐는지는 실기뿐. ⚠️미검증 섹션이 그 목록이다.
> 2. **웹 1:1 절대원칙** — 앱은 웹을 그대로 이식한다. "모바일이니까" 임의 재디자인 금지. 사용자가 가장 화내는 지점. 신규/수정 둘 다 웹 값을 본다 (`memory/web_app_parity.md`).
> 3. **추측 처방 금지** — 증상 신고 시 PNG·데이터로 추측 말고 grep/DB로 사실부터. "원본엔 없다/한쪽은 됐다"면 코드 분기부터 의심 (`memory/feedback_lying_patterns.md` 최신 사례).
> 4. **세션 끝 `/handover` 필수** — 안 하면 또 누락된다. 그게 이 보드를 만든 이유다.
> 5. 사용자는 런칭된 prod 운영 중. "MVP" 표현 금지. 데이터 분석엔 9번 팩트체커 자동 호출.
>
> **다음 세션 필독.** 이 보드(상단 3섹션)만 읽으면 이어서 할 일·누락이 0이 되도록 유지한다.
> 완료 기록 → `RELAY-LOG.md` · 완료 기능 상세 → `RELAY-ARCHIVE.md` · 옛 RELAY 본문 → git 히스토리.

---

## 🔴 NEXT — 지금 이어서 (우선순위순)

1. **앱 AI펫톡 남은 갭** (대조 에이전트 지목, 미착수): 펫 프로필 사이드바 / 내보내기 4종 템플릿(react-native-view-shot 의존성 결정 필요) / 푸시 배너(모바일 적합성 검토). 원칙: **웹 1:1 이식, 임의 재디자인 금지**.
2. **대화 저장 #4-b 중복행 보강** (9번 권고, 우선순위 낮음): `chat_messages` dedup_key UNIQUE 컬럼 + ON CONFLICT (재시도 시 중복행 방지, DB 마이그 필요). #4-a(`ai_chats` 동시성)는 저장 직전 union 병합으로 해결(아래 ⚠️미검증). **메멘토 철학상 유실<중복** — 유실은 차단됨, 중복은 데이터 살아있어 후순위.
3. **[경미] 타이핑 인디케이터**: 앱 PawLoading vs 웹 PawPrint 3개 rotate bounce. 현재 근접으로 스킵 판단. 사용자가 원하면 PawPrint 3개 staggered bounce로 정렬.

## ⚠️ 미검증 — 커밋ID / 검증레벨 / 남은 검증

- **웹 배포 후 시각확인 (L3→L5)**: AI펫톡 동물병원 환각차단 `818e317` · 알림 모달 `5853243` · 홈 카드 흔들림 `ab4c5a9` · /about 링크 `0b530ef` · 레벨 sparkle 제거 `e038927` · 소개 노출(온보딩/로그인/메타) `036a39a b6ad594` · CLAUDE 정정 `a52a762`(문서)
- **모바일 EAS 빌드 후 실기 (L2/L3→L5)**: 앱 AI펫톡 웹1:1 전체 `203bf3a 0f2b573 3213bb1 d70ae15 7771b03` (웹과 나란히 시각 대조) · 감정글로우 `7680f09` · auth refresh token `e0b9298` · 사진첩 스크롤/picker `549400a` · AI펫톡 crash `28d6496`
- **L4 미완 (API 응답)**: Tavily/네이버 강화 `9166e71`+`66119da` — "강아지 슬개골 영양제", "근처 동물병원" 등 실질문으로 구체성·환각無 확인 필요
- **#4-a 동시성 병합 (L2→L4)**: ai_chats 저장 직전 서버값 union 병합 (웹+앱 1:1, 9번 8/10). 웹+앱에서 **동시에 같은 펫 대화 후 양쪽 메시지 다 남는지** 실기 확인 필요. typecheck/build만 됨.
- **모바일 AI펫톡/사진첩 5건 (L2→L5)**: 타이핑 인디케이터 웹1:1 / 추천멘트 고정4개 / 입장스크롤 맨끝(웹도 통일) / 사진첩 getItemLayout. 9번 9/10. **Expo Go 실기**: 타이핑 말풍선·추천칩 정렬·입장 시 최근대화 즉시·사진첩 끝까지 부드럽게 확인. 웹은 배포 후 입장스크롤 맨끝 확인.

## ⏳ 외부대기 — 사용자/배포/빌드

- **EAS 빌드** (모바일 변경 다수 — 사용자가 빌드해야 위 모바일 미검증 해소)
- **Vercel 자동배포** (push 후 1~2분, 웹 미검증 해소)
- **CRON_SECRET 회전** (선택 — 채팅 노출 1회. 안 해도 운영 무방. 새 PAT는 2027-05-16 만료)
- **앱스토어 리스팅 정체성 리프레이밍** (Play Console 스크린샷·장문설명 — 코드 밖, 사용자 직접 작업. 7번 권고: 장례식장 오해 방어의 마지막 코드 밖 구멍. app.json 단문은 `036a39a`로 반영됨)
- ~~KCP MID 교체~~ → **해결 판명**: `src/lib/portone.ts` IP6S2 폴백 + dojin3497 실결제 imp_uid 확인. 옛 블로커 outdated.

---

## 🟢 미실행 마이그레이션

_(현재 없음 — 모든 마이그레이션 Supabase MCP로 prod 적용 완료)_

---

## 📋 보드 유지 규칙 (누락 방지의 핵심 — 반드시 준수)

1. **작업 완료 즉시**: NEXT/미검증에서 빼고 `RELAY-LOG.md` 맨 위에 `날짜 커밋ID 한줄요약` 추가. RELAY에 완료 항목 누적 금지.
2. **세션 끝마다 `/handover` 실행**: git log(마지막 인계 이후) ↔ 이 보드 자동 대조 → 보드에 없는 커밋·미반영 미검증 자동 검출 → 상단 갱신.
3. **이 파일 50줄 이내 유지**. 길어지면 완료분 LOG로.
4. **커밋 메시지가 진실의 원천**: 모든 커밋에 `검증: Lx` + 남은 검증 명시 (관행). 보드는 그 인덱스.
5. 영구 교훈 발생 시 → MEMORY.md 인덱스 + 본문 .md 둘 다 갱신 (인덱스만 갱신 금지).
