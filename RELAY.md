# RELAY — 인계 보드

> ## ✉️ 다음 세션에게 (먼저 읽어라) — 2026-06-11 인계
> **이번 세션(6/9~6/11) 한 일**: 펫홈 가입전환 Phase 0(①②③④) + Phase 1(공개펫홈 `/u/{nickname}`) 완료 / 사업계획서 가격 9,900 정합+SOM 검산 / 관리자 포인트 차감 / 게시글 이미지 라이트박스 / **전 코드베이스 DB-grounded 전수 감사(6영역)로 CRITICAL 다수 수술**. 모델: Phase는 Opus 4.8, 감사는 Fable 5(둘 다 커밋 co-author로 구분).
>
> **★다음 활성 작업 = 모두의창업 재도전 보완본** (사용자 합의·순차). NEXT 0 참조. ② 펫로스 완화 근거(continuing-bonds, **9번 통과 인용만** — 미검증 통계 통째 삭제, 디스클레이머≠안전) + ① BM 보강(가격 9,900 정합·AI영상 수익원·"이미 매출 발생 런칭"). 6/12 오프라인·6/22 온라인 멘토링 신청은 **사용자 직접**(가점 +1), 보완 반영 가점 +3, 7월초 2기 공고.
>
> 1. **전수감사 결과 배포검증 필요** — 이번 감사 수정(보안 RPC잠금·포인트부활·가격정합·모바일·웹)은 전부 L2~L3. **웹은 Vercel 자동배포 후 L5**(댓글 작성→포인트 +5P 적립, 차단 후 목록 갱신, 매거진 하트 유지), **모바일은 EAS 빌드 후**(알림 읽음, 게스트 가드). ⚠️미검증 참조.
> 2. **상시 규율 변함없음**: "AI가 자꾸 X" → 프롬프트 말고 grep 먼저(`feedback_lying_patterns.md`). DB/광범위/데이터분석 변경엔 9번 적대검증 강제(Stop hook). "MVP" 표현 금지(런칭 prod). 사용자 환경(콘솔클릭)은 1분 부탁 > 1시간 우회.
> 3. **사용자 컨텍스트**: 사업자번호 687-08-03135·개시일 2026.3.16·청년(1989)+1년이내 이중 우대. 정부지원 작업이면 우대트랙부터 (`user_seungbin.md`).
> 4. **전체 검수는 이제 `/audit` 스킬로** — 코드만 읽는 리뷰는 권한/죽은기능/RLS/stale을 영원히 못 잡는다(이번에 RPC노출·포인트 4개월 사망·가격잔존이 그래서 묵었음). 주기적으로 `/audit` 돌릴 것. prod DB 강제 대조가 핵심.
> 5. **재발방지**: RPC를 DROP+CREATE로 재생성하면 PUBLIC EXECUTE 부활(CREATE OR REPLACE는 ACL 보존) → 새 RPC 마이그마다 REVOKE PUBLIC+GRANT service_role. 옛 마이그 파일(예: 20260225 pg_cron)은 라이브와 달라 재실행 금지.
> 6. **세션 끝 `/handover` 필수**. 완료 기록 → `RELAY-LOG.md` · 완료 기능 상세 → `RELAY-ARCHIVE.md` · 옛 RELAY 본문 → git 히스토리.

---

## 🔴 NEXT — 지금 이어서 (우선순위순)

0. **★활성 — 모두의창업 재도전 보완본** (사용자 합의, 순차의 다음 차례). 목표=2기 가점 +3(심사의견 보완)+1(멘토링).
   - ② **펫로스 완화 근거** — ✅ **완료(2026-06-11)**: `docs/business/모두의창업_보완_펫로스근거.md`. continuing-bonds(Klass 1996)·disenfranchised grief(Doka 1989)·의례(Frontiers 2021 scoping review)·온라인 또래지지(Robinson&Pond 2019/Finucane 2025) + 필요성(Hyland 2026 PLOS One 7.5% PGD·8.1% PAR / KB 54.7%·83.2%·16.3%) → 기능 1:1 매핑. **9번 적대검증 SHIP WITH FIXES 전부 반영**(Norton&Gino는 Gino 데이터조작 논란으로 의도적 제외, 수치 정밀화). KB 83.2%/16.3%·Hyland는 이제 **출처검증 완료**라 사용 가능.
   - ① **BM** — **이미 초기창업패키지 PDF(`~/Downloads/안승빈 2026년도 초기창업패키지…pdf`)에 정직 버전 존재**: 4층 수익(구독 9,900/88,800·AI영상 4,900~34,900·B2B제휴·굿즈) + Unit Economics **LTV/CAC 7.4x(보수적 6개월, "확정값" 아님)**. 낡은 `*붙여넣기용.md`의 22.3x·"확정값"은 폐기. payments 실측=1명 8회 79,200원(활발한 매출 아님 → "결제 인프라 구축+실결제 검증"으로만 정직 표기). **남은 일=초기창업 PDF의 AI인재/영상내재화 앵글 빼고 BM골격만 모두의창업 2기 폼에 이식**(공고 7월초).
   - 멘토링 신청=**사용자 직접**(6/12 오프라인·6/22 온라인, 가점+1). 서울유니콘(6/30)에도 보완본 재활용.
0.3. **펫홈 Phase 0+1 완료 ✅ — 배포검증만 남음** (`8151cd7`/`ee71101`/`fdb5602`/`3264a9b`/`d736856`, 단일진실 `docs/PETHOME-SPEC.md`). Phase 0 ①가입후크 ②진짜목소리 ③게스트펫톡 ④퍼널 + Phase 1 공개펫홈 `/u/{nickname}`. 검증은 ⚠️미검증 참조. **보류**: 이웃(팔로우) 그래프 / N2(is_minihompy_private 1비트 오라클, 비노출 스키마 이전) / N3(비공개 펫홈 쓰기 가드) / 모바일 게스트 방명록 패리티 / 튜토리얼 라벨 전수.
0.5. **전수검수(2026-06-10~11) 잔여 후순위** (상세: RELAY-LOG 배치1·2 항목): ②펫톡 일일횟수 선차감 후 거절(실패에도 횟수 소모) ③스트리밍 delta에 sanitize 미적용(done만) ④subscription-renewal 멱등성(merchant_uid 랜덤 — 결제라 전용 작업) ⑤고아 파일 23건 삭제(AIChatLoginPrompt·logger·chat-context-builders 등, F 보고 목록) ⑥aiChat_rpm 행 무한 누적 정리 ⑦SubscriptionSection API 상수 일탈 ⑧increment_field RPC 부재 죽은 폴백 제거 ⑨N2(is_minihompy_private 1비트 오라클)·N3(비공개 펫홈 쓰기 가드). **재발방지 메모: RPC DROP+CREATE 시 PUBLIC EXECUTE 부활(CREATE OR REPLACE는 ACL 보존) / 20260225 pg_cron 마이그 파일은 auth 헤더 없는 구버전(라이브와 다름 — 재실행 금지).**
1. **EAS 빌드 → 베타 12명 신규 배포** (사용자 액션 필요): 5/19~5/20 모바일 11+커밋(웹1:1 패리티/PKCE/사진첩/펫 사진 히어로/추천멘트 정화/PawLoading/추천칩/감정글로우/스트리밍커서/채팅 회귀 fix 등)이 옛 AAB에 미반영. 사용자가 EAS 빌드 + Closed 트랙 AAB 업로드 + "출시 시작" 클릭. 그 후 베타 시각 검증으로 ⚠️미검증 모바일 묶음 해소.
2. **앱 AI펫톡 남은 갭**: (사이드바 `4be8fa9` · 내보내기 `a9e621f` L2완료 EAS실기대기 · 푸시배너=모바일 Expo Push 이미 구현, 이식 불필요)
3. **서울 유니콘 챌린지 신청** (마감 6/30): 농식품 사업계획서 자산 변형. 메모리얼 카테고리. constants.ts 가격/한도 동일 인용. 폐업이력 PR-B(2017~2024 스마트스토어) 약점 보강 필수.
4. **대화 저장 #4-b 중복행 보강** (9번 권고, 후순위): `chat_messages` dedup_key UNIQUE + ON CONFLICT (재시도 시 중복행 방지, DB 마이그). 메멘토 철학상 유실<중복 — `0599b7e`로 유실은 차단, 중복은 데이터 살아있어 후순위.
5. **[사용자 결정 대기] AI펫톡 헤더 구조**: 웹 헤더는 Select 드롭다운만, 모바일은 아바타+이름+서브문구+사용량배지. 1:1로 가려면 모바일에서 펫 정체성 제거해야 하는데 위험커서 임의 안 뜯음. 사용자가 "웹처럼 헤더 비워" 결정 시 진행.
6. **[보안 보류·Medium] 영상생성 쿼터 동시성(fal 비용)**: `video/generate` count체크→fal제출→insert 비원자 → 동시요청으로 무료1회 초과+fal 실비. 부분완화(rate-limit 5/분·쿨다운) 존재. 근본수정=pending row를 fal 제출 전 원자적 예약(신규 RPC+FOR UPDATE)+라우트 재구조화. **결제 수익기능이라 전용 작업+테스트 필요해 보류**(2026-06-04 보안감사).
7. **[보안 보류·Low] `increment_user_points` daily_cap TOCTOU**: COUNT 기반이라 동시요청으로 일일캡 살짝 초과 가능. **최고빈도 RPC라 blast radius 큼+심각도 낮아 보류**. 수정=함수 시작 시 `profiles FOR UPDATE` 직렬화(sell_minimi 패턴 동일).
8. **[사용자: 나중에] 조회수 고유 방문자 dedup**: 현재 1오픈=1조회(중복 수정 `5e43f66`)지만 같은 사람 재방문/새로고침은 매번 +1. 사용자가 "방문자 적으니 지금은 중복 OK, 나중에 중복 금지"로 보류. 구현 시: sessionStorage로 같은 글 재카운트 차단 + `views=views+1` 원자적 RPC(동시요청 누락 방지).
9. **[후순위] visit_logs 보관정책(retention)**: 방문 비콘(`4b91649` 이후)이 행 무제한 증가 → 오래된 행(예: 90일↑) 정리 필요. **단 Vercel Hobby cron 1일 1회 제약**(`session_20260420_21.md`)이라 기존 크론에 정리 쿼리 끼워넣거나 비콘에서 가끔 prune. 현재 저트래픽이라 긴급도 낮음. (인덱스는 생성됨)

## ⚠️ 미검증 — 커밋ID / 검증레벨 / 남은 검증

- **auth 레거시 deleted_accounts 경로 제거 (L2→L5, 2026-06-11)**: AuthContext/DeleteAccountSection에서 죽은 `save_deleted_account`·`check_deleted_account`·`mark_account_rejoined` 호출 전면 제거(live=withdrawn_users/can_rejoin는 무손). typecheck+build exit0. **배포 후: 회원가입·로그인·탈퇴·OAuth 콜백 정상 동작 + 콘솔 에러 無 확인**.

- **★전수감사 수정 배포 후 (L2/L3→L5, 2026-06-10~11)**: 보안 RPC잠금+포인트부활 `1cd5317` — **2026-06-11 prod 정적교차검증 완료(L3 PASS)**: `increment_user_points` 단일 올바른 오버로드·service_role 전용 잠금·txn insert 실측 + `awardPoints`(points.ts)가 6-arg로 호출 + 호출부 admin클라 주입·자추가드 확인. **행위적 L4/L5만 잔여** — 배포(6/10 16:11 KST) 후 적격 활동 거의 전무(point_transactions: 구매/로그인은 6/10 정상, 댓글 6/09 이후 0·채팅 6/02 이후 0·ai_chat 마지막 2/20)라 **댓글작성→+5P·출석토스트는 실활동/의도적 테스트 필요**. / 모바일 일괄 `7c51558`(EAS 후: 알림 읽음·게스트 가드) / 웹 차단필터·매거진하트·영상폴링 `d4bc8fd`(**차단 후 목록 갱신·하트 유지·영상 15초폴링 시각확인**) / AI·cron `890f082`(**vercel.json daily-greeting 제거 → 배포 후 pg_cron 단독발사 확인**, KST 경계 00~09시 생일/추모일) / 가격정합 `0cb4505`(구글 리치결과 리크롤 시간소요).
- **펫홈 Phase 0+1 배포 후 (L2/L3→L5)**: `/u/{닉네임}` 게스트 열람+OG / ③게스트펫톡 curl L4 / ④퍼널 데이터누적 후 대시보드 / ②③+공개펫홈 게스트 앱은 EAS 후.
- **DB 마이그 4건 적용됨 (prod, L3→L4)**: `lock_sensitive_rpcs`·`admin_adjust_points_atomic`·`visit_logs_funnel`·`minihompy_private_rls_tighten` — ACL/정책/시그니처 실측 완료, 실호출(curl) 동작은 배포 후.
- **웹 배포 후 시각확인 (L3→L5)**: AI펫톡 동물병원 환각차단 `818e317` · 알림 모달 `5853243` · 홈 카드 흔들림 `ab4c5a9` · /about 링크 `0b530ef` · 레벨 sparkle 제거 `e038927` · 소개 노출(온보딩/로그인/메타) `036a39a b6ad594` · CLAUDE 정정 `a52a762`(문서) · 추모 세션종료 넛지 30→60 `4b85121` · "..."+목소리금지+추천멘트 정화 `04b39ca`+`2df5428` · 영구 소실 차단 `0599b7e`
- **모바일 EAS 빌드 후 실기 (L2/L3→L5)**: 앱 AI펫톡 웹1:1 전체 `203bf3a 0f2b573 3213bb1 d70ae15 7771b03` · 감정글로우 `7680f09` · auth refresh `e0b9298` · 사진첩/picker `549400a` · AI펫톡 crash `28d6496` · 정밀 패리티 대조표 `868f31b` · 펫 사진 히어로+인사말 "..." `929b5ea` · 채팅 안 보임 회귀 fix `7201f85` · PKCE CSPRNG `53fef38` · 실기 재신고 3건 `0f0890c` · 실기 5건 `441c4a8` · cropPosition+스와이프 `4be8fa9` · 내보내기 4종 `a9e621f`. **실기 체크리스트**: 타이핑 웹1:1 / 추천칩 항상표시 / 입장스크롤 맨끝 / 사진첩 부드럽게 / 말풍선색 / AI펫 "..." 안 씀·목소리 언급 안 함 / 추천멘트 잘 자·목소리 안 뜸 / 히어로 카드+키보드 채팅 안 눌림 / **cropPosition 사진 보정 + 좌우 스와이프** / **내보내기 4종 외관+저장+공유** / OAuth 정상.
- **L4 미완 (API 응답)**: Tavily/네이버 강화 `9166e71`+`66119da` — "강아지 슬개골 영양제", "근처 동물병원" 실질문으로 구체성·환각無 확인.
- **#4-a 동시성 병합 (L2→L4)**: ai_chats 저장 직전 서버값 union 병합 (웹+앱 1:1, 9번 8/10). 웹+앱에서 **동시에 같은 펫 대화 후 양쪽 메시지 다 남는지** 실기 확인.
- **영구 소실 차단 실측 (L2→L4)**: `0599b7e` saveMessage 3회 재시도 + 백업. **저장 실패 시 backup 트리거되는지** 강제 fail 주입으로 실측 필요(현재는 정상 경로만 검증).

## ⏳ 외부대기 — 사용자/배포/빌드

- **EAS 빌드** (모바일 변경 누적: 기존 11+ 에 이번 세션 펫홈 ②③·이미지 라이트박스·공개펫홈 게스트·전수감사 일괄(`7c51558`) 추가 — 사용자가 빌드해야 모바일 ⚠️ 해소)
- **Vercel 자동배포** (push 후 1~2분 → 웹 ⚠️ 해소. 이번 세션 웹 커밋 다수 + DB마이그 4건 이미 prod 반영됨)
- **모두의창업 2기 멘토링 신청** (사용자 직접): 6/12 오프라인·6/22 온라인(최대 3회) → 가점 +1. 7월초 2기 공고.
- **농식품 창업콘테스트 결과 대기** (5/26 제출완료 → 서류 6.8~14 66팀 → 발표 6월중순 22팀 → 본선 7월초 11팀 → 결선 9월초·전문가80+모의투자20·1팀당 모의 5억. 청년+예비/1년이내 이중 우대.)
- **CRON_SECRET 회전** (선택 — 채팅 노출 1회. 안 해도 운영 무방. 새 PAT는 2027-05-16 만료)
- **앱스토어 리스팅 정체성 리프레이밍** (Play Console 스크린샷·장문설명 — 사용자 직접. 장례식장 오해 방어 마지막 구멍.)

---

## 🟢 미실행 마이그레이션

- (없음) — `20260604_fix_sell_minimi_unequip.sql`은 2026-06-11 MCP `apply_migration`로 prod 적용 완료(RELAY-LOG 참조). 4-param `v_equipped = v_delete_id::text` 교정 + ACL service_role 잠금 보존 실측 확인. L5(모바일 장착중 판매→해제)만 EAS 후 잔여.

---

## 📋 보드 유지 규칙 (누락 방지의 핵심 — 반드시 준수)

1. **작업 완료 즉시**: NEXT/미검증에서 빼고 `RELAY-LOG.md` 맨 위에 `날짜 커밋ID 한줄요약` 추가. RELAY에 완료 항목 누적 금지.
2. **세션 끝마다 `/handover` 실행**: git log ↔ 이 보드 자동 대조 → 누락 검출 → 상단 갱신.
3. **이 파일 50줄 이내 유지**. 길어지면 완료분 LOG로.
4. **커밋 메시지가 진실의 원천**: 모든 커밋에 `검증: Lx` + 남은 검증 명시. 보드는 그 인덱스.
5. 영구 교훈 발생 시 → MEMORY.md 인덱스 + 본문 .md 둘 다 갱신.
